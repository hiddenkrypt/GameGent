#include <SDL.h>
#include <stdio.h>
#include "gamegent_state.h"
#include "menus.h"


void Menus_draw(SDL_Renderer*  rend, gamegent_state currentGuiState){
    printf("currentGuiState: %d", (int)currentGuiState);
}
